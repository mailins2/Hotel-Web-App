<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ config('app.name', 'Hotel Web App') }}</title>
</head>
<body>
    <main style="font-family: Arial, sans-serif; max-width: 720px; margin: 48px auto; padding: 0 16px;">
        <h1>Hotel Web App</h1>
        <p>Trang mặc định không được sử dụng. Ứng dụng hiện chuyển hướng từ <code>/</code> sang <code>/dashboard</code>.</p>
    </main>
</body>
</html>
